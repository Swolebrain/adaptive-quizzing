<!DOCTYPE html>
<html>
  <head>
    <title>Adaptive Quizzing</title>
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style.css">
    <script src='js/vendor/angular.min.js'></script>
  </head>
  <body ng-app='LoginModule'>
    <div class='container'>
      <div class='row'>
        <div class='well bs-component col-xs-12 col-md-8 col-md-push-2'>
          <form class="form-horizontal" ng-controller='LoginController'>
            <fieldset>
              <legend>Please Log In my Nigga</legend>
              <div class="form-group">
                <label for="inputEmail" class="col-lg-2 control-label">Username</label>
                <div class="col-lg-10">
                  <input type="text" class="form-control" ng-model='username' placeholder="Username">
                </div>
              </div>
              <div class="form-group">
                <label for="inputPassword" class="col-lg-2 control-label">Password</label>
                <div class="col-lg-10">
                  <input type="password" class="form-control" ng-model='password' placeholder="Password">
                  
                </div>
              </div>
              <div class="form-group">
                <div class="col-lg-10 col-lg-offset-2">
                  <button type="reset" class="btn btn-default">Cancel</button>
                  <button class="btn btn-primary" ng-click='sendForm()'>Submit</button>
                </div>
              </div>
            </fieldset>
          </form>
        </div>
      </div>
    </div>
    <script>
      /*(function(){
        const app = angular.module('LoginModule', []);
        app.factory('UserService', ['$http', function($http){
          return {
            login: function(u,p){
              return $http.post('/api/users/login', {username:u, password:p});
            },
            getCurrent: function(){
              return $http.get('/api/users/current');
            }
          };
          
        }])
        .controller('LoginController', ['UserService', '$scope', 
        function(UserService, $scope){
          $scope.sendForm= function(){
            UserService.login($scope.username, $scope.password)
            .success((res)=>console.log(res));
          };
        }]);
      })();*/
    </script>
  </body>
</html>